import { Routes } from '@angular/router';
import { BlinkDetectorComponent } from './blink-detector/blink-detector.component';

export const routes: Routes = [
  { path: '', component: BlinkDetectorComponent }
];